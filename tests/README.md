# tests

Cross-cutting tests spanning more than one component of the system. Framework-
local unit/widget tests live alongside their own project (`frontend/test/`,
`backend/tests/`, `ai/tests/`); this folder is reserved for tests that verify
the system as a whole.
