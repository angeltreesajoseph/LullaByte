# training

Model training pipeline (SAD Section 11.5): dataset loading, train/validation/
test split handling, model architecture definition, and the training loop for
the Cry Analyzer classification model (PyTorch or TensorFlow, SAD Section
5.2).
