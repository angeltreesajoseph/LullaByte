# ai

The AI Prediction Service implementation (SAD Section 7.7, Section 11):
the Cry Analyzer classification pipeline and the AI Parenting Assistant
response-generation logic. Invoked exclusively by the FastAPI backend
(`backend/app/services/`), never directly by the Flutter client (SAD Section
3.2.4).
