# models

Model architecture definitions (SAD Section 11.5) for the Baby Cry Detection
gating classifier, the Non-Baby Sound classifier, and the primary cry-category
classification model — architecture/definition code only; trained weights are
stored separately in `ai/weights/`.
