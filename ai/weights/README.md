# weights

Versioned, trained model weight artifacts (SAD Section 11.9), named per the
`cry-classifier-v{n}` convention, with the currently active version referenced
from `ai/config/` rather than hardcoded — supporting rollback and the
Machine Learning Improvements future-scope item (SRS Section 16.4).
