# schemas

Pydantic schemas defining the request/response contracts for every API
endpoint (SAD Section 10.4), enforcing the same Validation Rules specified
per module throughout SRS Section 10 as the authoritative, server-side
check (SRS SEC-INPUT-01).
