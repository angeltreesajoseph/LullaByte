# middleware

Global HTTP middleware: CORS policy, request/response logging (SRS
NFR-MAINT-07), rate limiting (SRS SEC-API-01–02), and the global exception
handler translating unhandled errors into the standard error envelope (SAD
Section 10.5).
