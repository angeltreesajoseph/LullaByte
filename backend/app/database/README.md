# database

Database connection and session management for PostgreSQL (SAD Section 9,
Section 13.1), including the async engine/session factory consumed via
FastAPI dependency injection by `repositories/`.
