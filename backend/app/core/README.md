# core

Application configuration and startup wiring: environment-based settings,
FastAPI app factory, and the dependency-injection container binding
interfaces to concrete implementations across the backend (mirrors the
Dependency Injection pattern described in SAD Section 6.5).
