# auth

Cross-cutting authentication and authorization infrastructure (SAD Section
12.2–12.3): JWT issuance/validation, password hashing (SRS Section 14.2),
Firebase ID-token verification, and the shared `RoleGuard` FastAPI
dependency (SAD Section 10.3) consumed by every protected router.
