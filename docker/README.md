# docker

Container definitions for the deployment topology described in SAD Section
16.1–16.2: three independently deployable containers (backend, AI service,
reverse proxy) plus managed cloud services (PostgreSQL, Firebase, Cloudinary)
that are not self-hosted.
