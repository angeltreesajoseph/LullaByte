# ai

Dockerfile and container-build context for the AI Prediction Service, packaged independently so it can be scaled and redeployed without touching the backend container (SAD Section 3.4, Section 16.2).
