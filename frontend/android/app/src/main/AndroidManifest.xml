<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ================================================================
         LullaByte required permissions (SRS Section 12.8–12.11; SAD
         Section 12). Each is requested at runtime via permission_handler
         when the corresponding feature is used — declaring them here only
         makes the runtime request possible, it does not grant them.
         ================================================================ -->

    <!-- Internet access: required for all backend/API, Firebase, and
         Cloudinary communication (SRS Section 12.4–12.7). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Microphone: required for AI Cry Analyzer Live Recording
         (SRS Section 10.6.1, Section 12.8). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Camera: required for Gallery and Milestone photo/video capture
         (SRS Section 10.12, 10.11; Section 12.9). -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage/media access: required for Gallery upload and Cry Analyzer
         audio upload (SRS Section 10.6.2, 10.12; Section 12.10).
         Android 13+ (API 33+) uses the granular media permissions below;
         legacy storage permissions are capped at maxSdkVersion=32. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Notifications: required for the Notification System on Android 13+
         (SRS Section 10.17; Section 12.11). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Re-scheduling local notifications after a device reboot
         (SRS Section 10.17.8, Local Notifications). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:label="lullabyte"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
