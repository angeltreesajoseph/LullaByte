# error

Shared exception and failure types (e.g., `ValidationFailure`, `NetworkFailure`, `NotFoundFailure`) used by the Domain Layer across all features (SAD Section 14.1) to keep error handling consistent app-wide.
