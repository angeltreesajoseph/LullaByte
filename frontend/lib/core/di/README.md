# di

Dependency Injection wiring (SAD Section 6.5) — the central place where concrete repository/service implementations are bound to the abstract interfaces consumed by feature modules, via Riverpod providers.
