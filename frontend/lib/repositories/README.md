# repositories

Shared repository interfaces/base classes (Repository Pattern, SAD Section 6.2) used across multiple features; feature-specific repository interfaces and implementations live inside each feature's own `domain/repositories` and `data/repositories` folders.
