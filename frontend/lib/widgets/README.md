# widgets

Reusable, generic UI components shared across multiple features (buttons, form fields, cards, empty-state views), built to satisfy the accessibility requirements of SRS Section 15 (large tap targets, readable icons, color+label pairing).
