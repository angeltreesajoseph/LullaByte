# Baby Management — Presentation Layer

The View layer (MVVM, SAD Section 7.2) for the Baby Management feature. Renders UI
state exposed by `application/` and forwards user actions to it; contains no
business logic of its own.
