# Gallery — Presentation Layer

The View layer (MVVM, SAD Section 7.2) for the Gallery feature. Renders UI
state exposed by `application/` and forwards user actions to it; contains no
business logic of its own.
