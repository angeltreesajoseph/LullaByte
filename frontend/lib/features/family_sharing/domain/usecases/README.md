# usecases

Single-responsibility use-case classes for Family Sharing (e.g., "log a new entry", "calculate statistics"), each depending only on the abstract repository interfaces in `domain/repositories`.
