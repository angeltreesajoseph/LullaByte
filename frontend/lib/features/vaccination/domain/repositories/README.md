# repositories

Abstract repository interfaces for Vaccination Management (Repository Pattern, SAD Section 6.2) — contracts implemented by `data/repositories`, never depended upon directly by `data/`.
