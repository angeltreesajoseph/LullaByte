# usecases

Single-responsibility use-case classes for Notification System (e.g., "log a new entry", "calculate statistics"), each depending only on the abstract repository interfaces in `domain/repositories`.
