# usecases

Single-responsibility use-case classes for Growth Records (e.g., "log a new entry", "calculate statistics"), each depending only on the abstract repository interfaces in `domain/repositories`.
