# usecases

Single-responsibility use-case classes for AI Cry Analyzer (e.g., "log a new entry", "calculate statistics"), each depending only on the abstract repository interfaces in `domain/repositories`.
