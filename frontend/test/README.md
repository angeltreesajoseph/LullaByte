# test

Flutter unit and widget tests (framework convention — discovered automatically
by `flutter test`). Structure mirrors `lib/features/*` on a per-feature basis
once test files are added.

Distinct from the top-level `tests/` folder, which holds cross-cutting
integration, end-to-end, and performance tests spanning the whole system.
